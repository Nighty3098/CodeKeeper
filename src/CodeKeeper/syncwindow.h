#ifndef SYNCWINDOW_H
#define SYNCWINDOW_H

#include <QtWidgets>

class SyncWindow : public QMainWindow {
    Q_OBJECT

public:
    SyncWindow(QWidget *parent = nullptr);
    ~SyncWindow();

    private slots:

    protected:

    private:
};

#endif // MAINWINDOW_H
